# DPOP quantitative tests

This directory contains quantitative experiments for differentiated process
off-parameter particle filtering (DPOP). The reports are intended to document
numerical behavior, runtime, and comparison benchmarks for DPOP implementations
in `pypomp`.

The first experiment is the single-unit London measles benchmark in
`london_measles/`.
