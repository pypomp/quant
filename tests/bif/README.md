# BIF quantitative tests

This directory contains quantitative experiments for Bayesian iterated
filtering (BIF). The reports are intended to document posterior approximation,
runtime, and comparison benchmarks against PMCMC implementations in `pypomp`.

The first experiment is the SIR and Dacca PMCMC comparison in
`sir_dacca_pmcmc/`.
