repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.8
    hooks:
      - id: ruff
        name: ruff check (tests)
        files: ^tests/
        args: [--fix]
      - id: ruff-format
        name: ruff format (tests)
        files: ^tests/
